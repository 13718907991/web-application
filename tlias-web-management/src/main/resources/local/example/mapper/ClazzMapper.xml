<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="local.example.mapper.ClazzMapper">
    <select id="clazzList" resultType="local.example.pojo.Clazz">
        select clazz.id, clazz.name, room, begin_date, end_date, master_id,
        clazz.create_time, clazz.update_time, emp.name as masterName
        from clazz left join emp on clazz.master_id = emp.id
        <where>
            <if test="name != null and name != ''">
                clazz.name like concat('%', #{name}, '%')
            </if>
        </where>
        limit #{offset}, #{pageSize}
    </select>
</mapper>